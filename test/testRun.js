/* const auth = require('../src/services/authService');

(async () => {
    const authS = new auth();
    await authS.SignIn({
        email: 'mcc.code.test@mcc.com',
        pw: 'MccCodeTestPw1'
    })
})(); */